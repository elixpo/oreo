the next last big update is our device to be able to update itself from the internet if a big patch comwes, so we will release our OS in github packages called as the OreoOS, it shall be visible as a package in the repo with all the manifest and etc, a CI will be updating it, the link of the pull will be given to the badge, now the badge will be able to sometimes automaticlly pull in the resources from the web and show a system update screen when it's a small patch update, it will pull in the buffer and then show the user a message that it needs to reboot, and the user shall do that to reboot, user can manualy check for the version update form tge about app and settings app too and then update the bade, this way we can send an update (for example a new quest) or somerthing similar to the badges globally or any patch fixes or a new app etc... 